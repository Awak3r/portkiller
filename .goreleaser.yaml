version: 2

project_name: portkiller

builds:
  - env: [CGO_ENABLED=0]
    goos: [linux]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w
      - -X portkiller/internal/version.Version={{.Version}}
      - -X portkiller/internal/version.Commit={{.ShortCommit}}
      - -X portkiller/internal/version.Date={{.Date}}

archives:
  - formats: [tar.gz]


checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc